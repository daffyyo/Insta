<h1> My Trial Instagram </h1>
<% @posts.each do |post| %>
  <div class="row">
    <%= link_to (image_tag post.image.url(:medium)), post_path(post) %>
    <p>
      <%= post.caption %>
    </p>
  </div>
<% end %>
